# P1 运行时侦察报告 — 剪映 Windows 5.9.0.11632 函数级导出

> 日期: 2026-09-19 · 侦察工具: `hook_export_trace.js` + `re_probe_p1.py` (commit 14201dc)
> 数据: 渲染节点 6 轮侦察, 产出在 Linux `/tmp/p1probe/out*/`
> 结论: **函数级导出成立, 入口已锁定, 请求结构已画出主字段**

## 1. 渲染启动真相 (时间线实锤)

```
点导出按钮 → 导出面板打开 → ExportClient::exportStart 立即触发 (silent_export)
          → CreateFileW 创建 .__jianying_export_temp_folder__\<uuid>.mp4
          → ttvideoeditor.dll (Vulkan) + avformat-58 (FFmpeg) 渲染进行中
点确认导出 → 仅收尾确认 (渲染已在后台跑)
渲染完成   → temp 改名到 Videos\<draft>.mp4
关闭编辑器 → exportStart 再次触发 (第二次, 收尾阶段)
```

- 真实渲染入口 = `ExportClient::exportStart`（`videoeditor.dll` 导出符号, mangled 名可按名定位）
- 调用点恒定: `VECreator.dll+0x3040444`
- `NewVEWrapper::export_start` 存在但 UI 流程不经过它 (待利用的替代入口)
- 真实编码引擎 = `ttvideoeditor.dll` (626 导出, 纯 Vulkan vkCmd* 后端) + FFmpeg libavformat-58 (`Lavc61.31.101`)

## 2. lyra::ExportStartReqStruct 字段图 (RTTI 实锤 .?AUExportStartReqStruct@lyra@@)

| 偏移 | 类型 | 内容 / 语义 |
|---|---|---|
| +0x00 | ptr | vtable (0x7ffe..., videoeditor .rdata) |
| +0x08 | std::string | `"ExportService"` (RPC 服务名) |
| +0x28 | std::string | `"exportStart"` (RPC 方法名) |
| +0x48 | std::string | 短 token (残留 "VlA", 疑 app 类型) |
| **+0x50** | **std::string** | **输出路径** `F:\...\.__jianying_export_temp_folder__\<uuid>.mp4` ← macOS 参考的 +0x48 对应物 |
| +0x98 | u32 | 256 |
| +0xb0..0xd1 | packed | 混合小字段 (2 字节对齐), **+0xce = u32 10,000,000 (视频码率 10Mbps 候选)** |
| +0xe0 | ptr | 堆指针 (逐请求不同) |
| +0xf0/+0xf8 | double | 1.0 / 1.0 (疑似 scale_x/y) |
| +0x100 | u32 | 128,000 (音频码率 128k) |
| +0x108 | u32 | 2 |
| +0x110 | u32 | 18 |
| +0x118..0x134 | double×3+ | 1.0, 1.0, ...(0x3fc0e1e4 残片), 非整对齐 (packed/联合) |
| +0x150 | u32 | 11,289,600 (=0xAC4400, 疑采样率相关 11289600=256×44100) |
| +0x158 | u32 | 4096 |
| +0x180..0x358 | 混合 | SSO 混淆 token 串 (base64 残片, 跨请求不同 — 疑签名/nonce) |
| **+0x2F8** | std::string | `"silent_export"` |
| **+0x338** | std::string | `"unknown"` (与前者成对, 疑 key/value 标签) |
| +0x368..0x6c0 | 数组区 | 重复三元素组 (栈ptr+堆ptr+常量0x2c7eb5b4eb1), 疑 std::function/回调数组 |
| +0x6c8.. | char[] | 内嵌 GLSL/HLSL 着色器源码 ("uniform vector4 u_is_texture_file...") |

- 结构体真身约 0x700+ 字节, 之后是堆邻接区 (每次 dump 的"大小"变化即此故)
- 跨 dump 稳定字段已三份样本交叉验证

## 3. P2 施工方案 (按风险递增排序)

- **A. 飞行中补丁 (最低风险)**: hook exportStart onEnter, 改写 +0x50 输出路径字符串后放行 → 完全控制输出位置, UI 链路不动
- **B. 请求克隆重放**: 首次触发时深拷贝请求 (含指针目标内存), 同会话内再调用 `ExportClient::exportStart` 重放 → 一次 UI 交互, N 次渲染
- **C. 全构造直调**: 从零构造 ExportStartReqStruct (跳过导出面板点击) — 需弄清混淆 token 区 (疑签名) 是否可复用, 风险最高
- 建议顺序 A → B → C, 每步在 probe 环境验证后再进 render_driver

## 4. 与 macOS 参考项目的对照

| macOS (jianying-headless) | Windows 5.9 |
|---|---|
| `storage+0x48` 输出路径 string | +0x50 |
| `ExportService`/`exportStart` 字符串对 | 同 (+0x08/+0x28) |
| ToVeCompileSetting @+0x60 (width+0x3f...) | 未找到明文画布字段 — 5.9 按草稿画布渲染, 设置或在 token 区/其他消息 |
| restore_draft → exportStart 两段式 | 5.9 面板打开即 exportStart (silent 预渲染), 单段式 |
| libvideoeditor.dylib | videoeditor.dll (API 面) + ttvideoeditor.dll (Vulkan 渲染后端) |

## 5. 数据资产

- 内存 dump + 字段漫游 JSON: `/tmp/p1probe/out*/` (6 轮)
- `videoeditor.dll` 导出表: 8192 符号 (P0 报告)
- `ttvideoeditor.dll`: 626 导出 (Vulkan), 已拷至仓库根 (gitignored)

## 6. P2-A 飞行中补丁实验结果 (2026-09-19 晚, 3 轮节点实验)

### 实验记录
| 轮次 | 补丁目标 | 结果 |
|---|---|---|
| A1 | 两次 exportStart 都补丁到独立目录 `.__jianying_probe_out\p1_patched.mp4` | 引擎**确实写到补丁路径** (15,898,472 B 完整成片); 但 app 收尾按预持路径搬运找不到文件 → exportCancel + 自动重试(路径变 Videos\<uuid>.mp4) → 重试也被补丁 → 成片失踪 → 驱动超时 |
| A2 | 只补丁含 temp_folder 的请求(静默预渲染), 放过重试请求 | 完美: `p1_patched.mp4` 全尺寸成片落在补丁路径 **且** app 正常交付 rd*.mp4, exit=0 |

### 机制实锤
- **引擎写盘位置 = 请求 +0x50 字符串, 原位补丁(≤cap 覆盖)完全生效**
- **成片改名交付用 app 预持路径副本, 不读请求字符串** → 补丁交付类请求(#3, Videos\<uuid>)会断成片
- **确认点击的真实语义** = exportCancel(取消静默预渲染) + exportStart#3(Videos\<uuid>.mp4 真实交付请求) → 改名 rd<ts>.mp4
- **静默预渲染本身即完整成片** — 不点确认它也会渲染完 (补丁路径产物两次全尺寸实锤)

### P2-A 生产形态 (P3 集成方案)
```
注入草稿 → 打开草稿 → 点一次"导出"按钮
  → exportStart#1 onEnter 补丁 +0x50 → 任务输出路径
  → 驱动监视补丁路径至大小稳定 (代替确认/完成弹窗点击)
  → ESC 清弹窗 → 关编辑器 → 交付补丁路径成片
```
消掉确认弹窗+完成提示两次最脆交互; 交付不依赖 app 默认目录/改名逻辑.
遗留验证: 不点确认时预渲染是否必然完整跑完 (A1/A2 旁证低风险, P3 实测).

## 7. P3 生产集成 (2026-09-19 深夜, commit 29f7497) — 已上线

节点冒烟一次全绿 (rd1789823716323):
- hook_api.js 同 session 第二脚本装载 ✓ → 点一次导出按钮 ✓ → +0x50 补丁生效 ✓
- 引擎直写 VIDEOS\<草稿名>.mp4, 文件稳定判定完成, 整轮 60s (旧链 80-90s)
- 确认弹窗/完成提示弹窗交互全部消失; ESC 清面板无"放弃导出"弹窗
- 交付文件与传统链字节级同规格 (15,898,472 B)

安全网: 失败自动回退传统 confirm 链 (回退前清半成品防改名冲突) /
RENDER_EXPORT_MODE=ui 强制旧链 / 关编辑器前先关补丁 (防 close-time
exportStart 重渲染覆盖交付文件).
剩余交互: 点卡片开草稿 + 点一次导出按钮 (PostMessage 到按名定位按钮).
