# === 渲染节点配置 (render_service) ===
# 这是 render 节点专用配置. 渲染节点不需要大模型/语音识别密钥
# (那些是 web 后端的事), 所以本文件不含 QWEN_API_KEY / ASR_API_KEY.

# 本机 render_service 监听地址/端口.
#   0.0.0.0 = 允许局域网内其他机器访问 (推荐, 这样 web 后端才能连过来)
#   127.0.0.1 = 只允许本机访问 (仅本机测试用)
RENDER_SERVICE_HOST=0.0.0.0
RENDER_SERVICE_PORT=9020

# 调用令牌 (X-Render-Token).
#   强烈建议自设一个长随机串 (见 start_here.html 第 3 步的生成方法),
#   并把同一个串填到 web 后端 "Render Node" tab 的 token 框里.
#   留空 = 不校验令牌 (仅本机测试用, 不安全, 不建议).
RENDER_SERVICE_TOKEN=

# === 剪映路径 (默认按当前用户推导, 正常安装剪映后无需改) ===
# 如果你把剪映装到了非默认位置, 才需要取消注释并改这三行:
# JY_APP_BASE=C:\Users\<你>\AppData\Local\JianyingPro\Apps
# JY_DRAFT_ROOT=C:\Users\<你>\AppData\Local\JianyingPro\User Data\Projects\com.lveditor.draft
# VIDEOS_DIR=C:\Users\<你>\Videos

# === 渲染参数 (一般无需改) ===
# 单任务渲染超时 (秒)
RENDER_TIMEOUT=600
# 任务记录保留时长 (秒)
TASK_TTL=86400
# 隔离桌面列表 (逗号分隔, 每个桌面 = 1 路并行渲染; 默认 1 路够用)
DESKTOP_NAMES=JYRender_0
# 校准坐标预设窗口尺寸
PRESET_W=1280
PRESET_H=720
